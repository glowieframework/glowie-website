# Requirements
In order to run Glowie you will need a web server that meets the following requirements:

- PHP version **7.4.16** or higher with `curl`, `mbstring` and `mysqli` extensions
- Apache version **2.4.35** or higher with `mod_rewrite` enabled
- Composer version **2.0.9** or higher

If you are planning to work with databases, you will also need:

- MySQL **5.7.24** or higher